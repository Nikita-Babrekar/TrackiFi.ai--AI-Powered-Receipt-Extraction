# AI Receipt Tracker - Frontend

This is a modern React frontend built with Vite. It features a drag-and-drop file uploader and dynamic UI to interact with the AI Receipt Tracker API.

## Setup Instructions

1. **Install dependencies:**
   ```bash
   npm install
   ```

2. **Run the development server:**
   ```bash
   npm run dev
   ```

3. **Open your browser:**
   Navigate to `http://localhost:5173` (or the URL shown in your terminal).

Ensure the Python Flask backend is running on `http://localhost:5000` to handle file uploads.
